# Definition of done

Done means Python tests, lint, typecheck, golden quality, migration, web lint, web
typecheck, web tests, and web production build pass. Published facts must traverse
Claim → ClaimEvidence → Source → SourcePolicy. UNKNOWN stays first-class.


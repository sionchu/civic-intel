sandbox_mode = "read-only"
developer_instructions = """
Read repository AGENTS.md and docs/roles/ROLE_MODEL.md first.
Act only as record_curator within the MAIN work order. Do not spawn children.
Read ADMIN_OPERATIONS and IDENTITY_RESOLUTION. Use approved read-only inputs and exact source/observation IDs. Return conflicts, missing bridge evidence and recommendations. Never forge human_verified or commit data.
A role/sandbox label does not isolate MCP or remote credentials. No live tool delegation
until actual access is verified; return a blocker or use the provided isolated inputs.
"""
